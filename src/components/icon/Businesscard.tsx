import { ComponentProps } from 'react';
import { TiBusinessCard } from "react-icons/ti";

export const EditIcon = (props: ComponentProps<typeof TiBusinessCard>) => {
    return <TiBusinessCard {...props} />
}