import { MantineSpacingValues } from "@mantine/core";
import { minshumiTheme } from "../theme";

export const spacing: MantineSpacingValues = {
    xs: `${minshumiTheme.spacing.xs}px`,
    sm: `${minshumiTheme.spacing.sm}px`,
    md: `${minshumiTheme.spacing.md}px`,
    lg: `${minshumiTheme.spacing.lg}px`,
    xl: `${minshumiTheme.spacing.xl}px`,
}
