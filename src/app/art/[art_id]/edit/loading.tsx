import { Loader } from "@/components/Loader"
import { Center } from "@mantine/core"

const Loading = () => {
    return (
        <Center>
            <Loader />
        </Center>
    )
}
export default Loading
